#include <bits/stdc++.h>
using namespace std;
#define fori(i, a, n) for(int i =a; i<n;i++) 
#define endl '\n'

void solve(){
    int n, aux; 
    vector<int> R, L;
    cin >> n;

    fori(i, 0, n){
        cin >> aux;
        R.push_back(aux);
    }
    fori(i, 0, n){
        cin >> aux;
        L.push_back(aux);
    }

    
}

int main(){
    //fastIO();
    solve();
    return 0;
}
